/**
 * When updting this list; make sure to update the
 * corresponding breakpoints found in the _variables.scss
 * file as well so that breakpoints are synchronised between
 * both the TypeScript and the SCSS.
 */
const breakpoints = {
    small: '425px',
    medium: '720px',
    large: '940px',
    maxWidth: '1400px'
}

export default breakpoints
